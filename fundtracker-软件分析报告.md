# FundTracker 软件分析报告

> 分析对象：`/Users/sheng/Desktop/fundtracker`（本地基金追踪桌面应用）
> 分析日期：2026-08-14
> 方法：全量源码阅读 + SQLite 真实数据抽样核验（数据库当前 163 只基金 / 156 条持仓 / 1202 条持仓股 / 40 条 OCR 日志）

---

## 一、软件定位与核心价值

一个**纯本地运行**的基金持仓追踪工具（Electron 桌面壳 + 本地 SQLite + 本地 Express API）。核心卖点是两条：

1. **隐私优先**：数据全部存本地 SQLite，不上传任何云端；API 仅绑定 `127.0.0.1:3456`。
2. **截图即持仓**：用 `tesseract.js` 本地 OCR 识别支付宝 / 京东金融 / 腾讯理财通的持仓与交易截图，识别结果可逐项核对修正后入库——解决了"手动录入基金太麻烦"的痛点。
3. **盘中实时估值**：基于基金季报/年报披露的前十大重仓股占比 + 成分股实时行情，估算本人基金的盘中涨跌幅，弥补了平台"盘中不显示估值"的空白。

目标用户画像清晰：**多平台（支付宝/京东/理财通）分散持有基金、且对隐私敏感的个人投资者**。这是一个真实且有差异化的需求，不是玩具项目。

---

## 二、架构总览

```
Electron 主进程 (main.js)
   └─ 启动 Express API (127.0.0.1:3456) + 打开窗口 (loadURL)
        ├─ 路由层   server/index.js          REST API（约 18 个端点）
        ├─ 数据层   db/db.js                 sql.js(WASM SQLite) + better-sqlite3 兼容层
        ├─ 业务服务 services/
        │     ├─ quotes.js     实时行情（新浪主 / 东方财富备，自动回退）
        │     ├─ fundInfo.js   基金净值 + 季报持仓（东方财富 F10）
        │     ├─ ocr.js        截图 OCR + 规则解析（最大最复杂模块）
        │     └─ estimator.js  实时涨跌幅估算引擎 + 组合汇总
        └─ 前端     web/ (index.html + app.js + styles.css)  原生 JS，无框架
```

**技术选型评价**：选型务实——`sql.js` 规避了 `better-sqlite3` 的原生编译坑；行情/基金数据全部用无需 key 的公开接口；OCR 纯本地。模块边界清晰，注释详尽（中文），整体工程质量在个人项目中属于**中上水平**。

---

## 三、技术亮点（值得肯定）

- **行情接口容错**：新浪失败自动回退东方财富，单只失败静默忽略，不阻断整体。
- **OCR 解析相当精细**：针对 OCR 把千分位逗号误识别成点、金额粘连进基金名、收益率数字污染名称等大量边界情况做了专门处理；支付宝"只有名称无代码"的场景还能回查代码补全。
- **安全基础做得对**：`contextIsolation: true` + `nodeIntegration: false` + CSP 限制 `connect-src` 仅本机——Electron 安全三板斧到位。
- **估值方法论诚实**：README 明确声明"仅含股票仓位、债券现金近似零波动、与官方估值有偏差"，没有夸大精度。

---

## 四、已发现的问题（均为数据库实证）

### 4.1 估值覆盖存在明显缺口（最严重）

| 问题 | 证据 | 影响 |
|------|------|------|
| **约 1/4 基金完全没有持仓股记录** | 163 只基金中仅 123 只有 `fund_stock`，**40 只缺失（24.5%）** | 这些基金盘中估值恒为 0%，组合汇总被"静默拉平"，用户以为没波动 |
| **两只 QDII 联接基金持仓为空** | `012348` 天弘恒生科技ETF联接、`014424` 博时恒生医疗保健ETF联接 的 `fund_stock` 均为 **0 行** | 这两只是用户重仓（各 8k+），但日内估值永远 0%——而它们实际跟随港股大幅波动，**严重误导** |
| **持仓报告期严重过期** | `fund_stock` 中混入 `2019Q3`、`2023Q4`、`2024Q1/Q3` 等陈旧数据（共 23 条） | 用 2019 年的持仓快照估算 2026 年净值，误差极大；刷新逻辑未校验"报告期是否够新" |

> 根因推测：东方财富 F10 对"ETF联接 / QDII"类基金的持仓 HTML 结构与普通开放式基金不同，`parseHoldingHtml` 只取第一个 `<tbody>`，可能在这些基金上漏解析；陈旧数据则是刷新失败时保留了旧值且无时效校验。

### 4.2 字段形同虚设

- **`fund.type` 全部为空**（163/163）：`refreshFundInfo` 调用天天基金主接口拿到的 `fundtype` 为空、备接口根本不返回类型，导致"类型"列永远显示 `--`。
- **`txn`（交易记录）表为空**（0 行）：交易截图导入链路在真实数据中**从未产出过记录**，说明该功能要么未被使用、要么存在隐患，属于**未经验证的代码路径**。
- **`stock_quote` 表是死表**：声明了但估算时每次都实时拉行情，从不写入/读取该表。

### 4.3 性能瓶颈

`estimatePortfolio` 在循环里**逐只基金调用 `getRealtimeQuotes`**（每只一次新浪请求）。按当前 123 只有持仓的基金、每只约 10 只成分股，单次 `/estimate` 会发起 **~123 次 HTTP 请求**。组合稍大就易触发新浪限频或明显变慢。应把所有成分股代码汇总后**一次性批量拉取**。

### 4.4 安全暴露面（本地应用特有风险）

- `app.use(cors())` 开启了 `Access-Control-Allow-Origin: *`。前端本就同源（同一 Express 提供静态资源），**CORS 既多余又危险**：用户在浏览器访问任意恶意网站时，该站点可跨域 `fetch('http://127.0.0.1:3456/api/holdings')` 读取你的全部持仓，甚至 POST 篡改。建议直接移除 cors 中间件（同源无需），或至少限制 Origin。
- `/api/upload` 接受任意二进制写入磁盘 + `/api/ocr` 读取该路径，恶意站点可借此消耗本地磁盘 / 触发本机 OCR 计算。本地绑定 127.0.0.1 缓解了外网风险，但 localhost 攻击面仍应收窄。

### 4.5 潜在逻辑缺陷（尚未在数据中触发，但存在）

- `normalizeCode` 只处理 `sh/sz/bj`，**不识别港股代码**（如 `00700` 腾讯）。一旦某基金持仓含港股且被解析出来，会被错误映射成 `sz00700`。当前数据恰好无港股代码（因 QDII 基金持仓解析失败），故未触发，但是一颗定时炸弹。

### 4.6 其他工程欠账

- **零自动化测试**：仅 `ocr.js` 有一个手动自测入口，无单元测试 / 集成测试。
- **`cost_amount` 有 7 条为 NULL**：个别持仓无法计算收益率（影响有限）。
- OCR 名称偶发残缺：数据库中可见 `天弘恒生科技ETF联接(QDIDC` 这类被截断/误识的名称（已被 `name_matched` 真实名称兜底，属展示瑕疵）。
- 当前 `node_modules` 未安装、`electron` 二进制未下载，**无法直接运行**，需先 `npm install`（首次会下载 Electron 与 tesseract 语言包，耗时较长）。

---

## 五、改进建议（按优先级）

1. **【高】修复估值覆盖**：`refreshFundInfo` 后校验"持仓股是否非空 + 报告期是否 ≧ 最近一年"，失败则标记 `est.empty` 并在前端显式提示"该基金无法盘中估值"，而非静默 0%。
2. **【高】修 QDII/联接基金解析**：针对东方财富 F10 对 ETF联接/QDII 的不同 HTML 结构做适配，或改用"联接基金→跟踪指数→指数成分股"的间接估值路径。
3. **【高】收紧 CORS**：移除 `cors()` 或限定 Origin；为本地 API 增加最小校验（如仅接受来自本应用前端的请求）。
4. **【中】行情批量拉取**：`/estimate` 改为一次性汇总全部成分股代码批量请求，降请求数 1~2 个数量级。
5. **【中】补全 `type` 字段**：从东方财富基金_search 或详情接口补全基金类型。
6. **【中】验证交易截图导入**：补齐 `txn` 落地链路并测试（当前为空表）。
7. **【低】港股代码归一化**：`normalizeCode` 增加港股（`hk` 前缀 / 5 位代码）分支。
8. **【低】补关键路径测试**（至少 estimator 与 ocr 解析），清理死表 `stock_quote`。

---

## 六、总体评价

**定位精准、隐私友好、OCR 与行情容错做得到位，是一个有真实使用价值的方向。** 短板集中在"估值覆盖率与数据时效性"——当前约 1/4 基金无法估值、含用户重仓的 QDII 基金估算恒为 0%，这会直接动摇核心功能的可信度。其次是本地 API 的 CORS 暴露面与零测试的工程债。

建议优先啃下"估值覆盖"（4.1）与"安全收窄"（4.4）两块，其余为体验与健壮性增强。若需要，我可以基于以上分析直接给出修复代码或制定改造方案。
