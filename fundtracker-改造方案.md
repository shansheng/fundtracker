# FundTracker 改造方案

> 文档定位：基于《FundTracker 软件分析报告》的落地改造规划
> 产出视角：MvpDevExpertTeam · 架构规划（非 MVP 从零搭建，而是既有项目的增量改造）
> 日期：2026-08-14

---

## 0. 改造目标与约束

### 0.1 目标
在**保留三大核心优势**（纯本地隐私优先、截图 OCR 自动录入、季报重仓股盘中估值）的前提下，集中解决分析报告指出的两大致命短板：

1. **估值覆盖率与时效性**（约 1/4 基金无法估值、用户重仓 QDII 恒为 0%）。
2. **本地 API 安全暴露面**（CORS 全开、upload/ocr 无防护）。

并顺带补齐工程债（性能、字段、死表、测试）。

### 0.2 硬性约束（不可逾越）
- **不引入任何云依赖**：数据不出本机，不改 API 绑定 `127.0.0.1` 的设计哲学。
- **不破坏现有数据**：所有 schema 变更向后兼容，迁移脚本可回滚。
- **增量改造**：每个 Phase 独立可交付、可验证，避免一次性大改导致不可运行。
- **优先安全、后功能**：先把暴露面收窄，再做功能增强。

---

## 1. 问题 → 方案映射

| # | 报告问题 | 优先级 | 改造模块 | 方案要点 |
|---|----------|--------|----------|----------|
| 1 | 40 只基金无持仓股 / 估值恒 0% | 高 | A. 估值覆盖 | 适配联接/QDII 解析 + 间接估值路径 |
| 2 | 重仓 QDII 联接基金 `fund_stock` 为 0 行 | 高 | A | 联接基金→跟踪指数→指数成分股估值 |
| 3 | 持仓报告期过期（2019Q3/2023Q4…） | 高 | A | 时效校验 + 过期显式提示 + 强制刷新 |
| 4 | CORS 全开，恶意站点可跨域读写 | 高 | B. 安全收窄 | 移除/限定 CORS + upload 白名单 |
| 5 | `/estimate` 逐基金发请求（~123 次） | 中 | C. 性能 | 批量行情 + 行情缓存 |
| 6 | `fund.type` 全空 | 中 | D. 字段补全 | 补全类型字段 |
| 7 | `txn` 交易表 0 行（未验证路径） | 中 | D | 打通并测试交易导入链路 |
| 8 | `normalizeCode` 不识港股代码 | 低 | E. 代码归一化 | 增加 hk/us 分支 |
| 9 | 零自动化测试 / 死表 `stock_quote` | 低 | F. 工程债 | 引入 vitest + 复活或清理缓存表 |

---

## 2. 分模块改造设计

### 模块 A · 估值覆盖率与时效性（最高优先）

**A1. 联接基金 / QDII 的持仓解析（推荐双路径）**

根因：东方财富 F10 对 ETF 联接、QDII 类基金的持仓 HTML 结构异于普通开放式基金，`parseHoldingHtml` 仅取第一个 `<tbody>` 导致漏解析。

- **路径 1（直接解析增强）**：`parseHoldingHtml` 不再硬编码"第一个 tbody"，改为按表头关键字（"股票代码/持股数/占净值比例"）定位真实持仓表；兼容多个 `<tbody>`。
- **路径 2（间接估值，主推）**：联接基金本质复制跟踪指数/ETF 的走势。新增 `refreshTrackIndex(fundCode)`：
  1. 从基金详情接口取"跟踪标的指数代码"（如 `HSTECH` 恒生科技、`HSHCI` 恒生医疗保健）；
  2. 查该指数成分股及权重（东方财富指数行情页）；
  3. 写入 `fund_stock`，`source = 'index'`、`ratio` 取成分股权重。
- **调度**：`refreshFundInfo` 先尝试路径 1；若持仓为空，自动回退路径 2；仍失败则标记 `estimatable = 0`。

**A2. 报告期时效校验**

- 新增 SQLite 列：`fund_stock.report_period`（已有）、`fund_stock.fetched_at`（时间戳）。
- 刷新后逻辑：`report_period` 距今 > 365 天 → 置 `est.stale = 1`。
- 前端：过期基金在估值卡片显示"⚠ 持仓数据过期（2023Q4），估值仅供参考"，而非静默使用。
- 自动刷新策略：检测到"当前季度 ≠ 最新报告期"且距上次刷新 > 30 天，置"建议刷新"标记；提供手动"强制刷新全部"按钮。

**A3. 空持仓不再静默 0%**

- `estimatePortfolio` 对持仓为空的基金返回 `{ estimatable: false, changePct: null }`。
- 组合汇总时：排除不可估值部分，并在汇总卡标注"共 N 只基金未纳入盘中估值（占组合 X%）"，杜绝被 0% 静默拉平。

**新增/变更字段**
```
fund.track_index_code  TEXT     -- 跟踪指数代码（联接/QDII）
fund_stock.source      TEXT     -- 'direct' | 'index'
fund_stock.fetched_at  INTEGER  -- 拉取时间戳
```

### 模块 B · 本地 API 安全收窄（高优先）

**B1. CORS 处理**
- 移除 `app.use(cors())`（前端同源由 Express 静态托管，无需 CORS）。
- 若后续确实需要跨域（如独立前端调试），改为白名单：`origin: ['http://127.0.0.1:3456']`。

**B2. upload / ocr 防护**
- 文件类型白名单（png/jpg/jpeg/webp），大小上限（如 10MB）。
- 写入路径随机化（`tmp/<uuid>.ext`），处理完即删；禁止路径穿越。
- `/api/ocr` 仅接受本应用写入的路径引用，不直接接收裸 base64 大块（或严格校验尺寸）。

**B3. 监听绑定确认**
- 确认 Express 仅 `listen(3456, '127.0.0.1')`，无 `0.0.0.0` 暴露。

### 模块 C · 行情性能（中）

**C1. 批量拉取**
- 汇总单次 `/estimate` 涉及的全部成分股代码，调用东方财富批量行情接口一次获取（替代逐基金循环新浪请求）。
- 预期：请求数从 ~123 降到 1~2 个数量级。

**C2. 行情缓存（复活 `stock_quote`）**
- `stock_quote` 表改为短期缓存：字段 `code, price, changePct, updated_at(TTL=30s)`。
- 估算前先查缓存，命中且未过期则跳过网络请求；批量刷新写入缓存。
- 解决报告中"死表"问题，同时支撑性能。

### 模块 D · 字段补全与死表治理（中）

**D1. 基金类型**
- `refreshFundInfo` 从东方财富基金详情/搜索接口补全 `fund.type`（股票型/混合型/债券型/QDII/指数型…），前端"类型"列不再显示 `--`。

**D2. 交易导入链路打通**
- 审查 `ocr.js` 交易截图解析分支；补齐 txn 落库（amount/cost/date/nav）。
- 提供一条端到端测试数据，验证"交易截图→入库→收益率计算"全链路（当前为空表属未验证路径）。

**D3. 死表处理**
- `stock_quote` 按 C2 复活为缓存表；否则删除并清理引用。

### 模块 E · 代码归一化（低）

**E1. `normalizeCode` 增强**
```js
// 现有：sh/sz/bj
// 新增：
if (/^\d{5}$/.test(code)) return 'hk' + code;        // 港股 5 位
if (/^[a-zA-Z]/.test(code)) return 'us' + code;       // 美股字母
```
**E2. 行情接口扩展**
- 港股：`rt_hk00700`；美股：`gb_${code}`（新浪前缀），并入批量与回退逻辑。

### 模块 F · 测试与工程债（低）

**F1. 引入 vitest**
- 覆盖：`estimator.estimatePortfolio`（含空持仓/过期/混合路径）、`ocr` 解析（用报告里提到的"逗号误识/金额粘连"样例）、`normalizeCode`、`parseHoldingHtml`。
- 数据：`tests/fixtures/` 放样例 HTML / 截图文本。

**F2. 可运行性**
- 明确 `npm install`（含 Electron + tesseract 语言包耗时）；README 补充首次运行须知与一键启动说明。

---

## 3. 数据模型变更汇总

| 表 | 变更 | 兼容方式 |
|----|------|----------|
| fund | 新增 `track_index_code` | `ALTER TABLE` 加列，旧行为 NULL |
| fund_stock | 新增 `source`、`fetched_at` | 加列，旧行 `source='direct'`、`fetched_at=0` |
| stock_quote | 复活为缓存表（TTL 列） | 重建表结构 |
| （无破坏性删除） | —— | 仅增列/复用，不删用户数据列 |

---

## 4. 实施路线图

| Phase | 主题 | 关键交付 | 预估工期 |
|-------|------|----------|----------|
| **Phase 0** | 安全兜底 | 移除/限定 CORS、upload 白名单、监听绑定确认 | 0.5–1 天 |
| **Phase 1** | 估值核心修复 | A1 双路径解析、A2 时效校验、A3 空持仓显式提示 | 3–5 天 |
| **Phase 2** | 性能与字段 | C1 批量行情、C2 行情缓存、D1 类型补全、D2 交易链路 | 2–3 天 |
| **Phase 3** | 健壮性 | E 港股归一化、F1 测试、F2 运行文档 | 2 天 |
| **验收** | 全量回归 + 真实数据抽检 | 见第 5 节 | 0.5 天 |

> 建议顺序：**Phase 0 → 1 → 2 → 3**。Phase 0 风险最低、收益安全，可立即动手；Phase 1 直接命中核心可信度问题。

---

## 5. 验收标准（可量化）

1. **估值覆盖**：改造后抽样 163 只基金，无法估值的占比从 24.5% 降至 < 5%；用户两只重仓 QDII（`012348`/`014424`）能产出非零盘中估值。
2. **时效**：任一基金持仓报告期 > 1 年，前端必须显式"过期"提示，不得静默使用。
3. **安全**：用浏览器独立页面 `fetch('http://127.0.0.1:3456/api/holdings')` 跨域请求应被浏览器拦截（CORS 已收窄）；`/api/upload` 非白名单类型返回 4xx。
4. **性能**：`/estimate` 单次网络请求数 ≤ 3（批量 + 缓存命中）。
5. **字段**：`fund.type` 非空率 > 90%；`txn` 链路至少一条端到端成功样例。
6. **测试**：`npm test` 通过，覆盖 estimator / ocr / normalizeCode，关键路径覆盖率 ≥ 60%。

---

## 6. 风险与回滚

- **数据迁移风险**：所有变更仅 `ALTER TABLE` 加列，不删列；提供迁移脚本，回滚即删除新增列（或整体冷备 `fundtracker.sqlite`）。
- **指数成分股接口变动**：路径 2 依赖东方财富指数页结构，若失效自动回退"直接解析"，最终标记 `estimatable=0` 而非崩溃。
- **批量行情限频**：C1 增加失败回退到逐只请求，保证可用性优先。

---

## 7. 建议的下一步

方案确认后，建议从 **Phase 0（安全兜底）** 立即动手——改动小、风险低、收益明确。需要的话我可以：
1. 直接实现 Phase 0（移除 CORS + upload 白名单）；或
2. 先把 Phase 1 的估值核心修复写成更细的代码级设计（含 `refreshTrackIndex` 接口契约）再动手。
